// Define a Playlist class. It should have...
  // A title property that is determined by some input.
  // A songs property that is determined by some input. It should contain multiple songs, each of which can just be a song title.
  // A favorites property that is initialized at 0. This cannot be set by user input.
  // An addSong method that adds a song to the songs property.
// var playlist = {
//   name : ['SuperBowl']
//   songs: ["Start Me Up", "Mary Janes Last Dance", "Bad Romance"]
//   played: 0,
// }

playlist = new Object();

var name = "Superbowl";
var songs = ("Start Me Up", "Mary Janes Last Dance", "Bad Romance");
var numberOfTimesPlayed: 0;

// Create an instance of the Playlist class.
function.Playlist(name, songs, numberOfTimesPlayed) {
  this.name = name;
  this.songs = songs;
  this.numberOfTimesPlayed: timesplayed;
}
var playlist1 = new Playlist("celebration", "Unspoken", 0)
// var playlist = object.create(playlist)
// Create an Album class that inherits from Playlist. It should also...
var album = Object.create(Object.create(playlist));

  // Have an artist property that is determined by some input.

// Create an instance of the Album class.
